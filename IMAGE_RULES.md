# 포켓몬 이미지 생성·등록 규칙

`polos0117/pkm-atelier` 에서 캐릭터 이미지를 만들고 도감에 등록하는 AI 와 작업자를
위한 선행 문서다. 생성 전, 업로드 전, 이름을 붙이기 전, 중단된 작업을 다시 잇기 전에
읽는다. 앞선 저장소(`atelier`)의 `IMAGE_UPLOAD_RULES.md` · `PROMPT_IMAGE_WORKFLOW.md`
를 이 프로젝트의 꼴(한 캐릭터 = 장갑 셋 + 개방 셋 + 일상 하나, 그림 저장소 분리)에
맞춰 다시 쓴 것이다. 생성기 자체의 규칙은 [docs/PROMPT_REWRITE.md](docs/PROMPT_REWRITE.md)
에 있다.

## 1. 반드시 지킬 원칙

- **기준 시트 한 장이 그 캐릭터의 정본이다.** 후속 결과를 다음 생성의 새 기준으로
  갈아 끼우지 않는다. 일곱 장 모두 처음 확정한 기준 시트를 첨부해 만든다.
- 기준 시트는 **생성 작업용**이다. 도감·전투에 쓰는 이미지가 아니며 `img/` 에 올리지
  않는다(올리면 등록기가 "그런 카드가 없다" 로 막는다). 따로 보관한다(4절).
- 화풍은 **캐릭터마다 하나로 고정**한다. 일곱 장은 같은 화풍 키로 만들고, 파일명에
  그 키를 그대로 넣는다. 화풍 키는 `data/style.json` 의 영문 키만 쓴다.
- 외형을 보고 짐작한 화풍과 생성기에서 실제 고른 화풍을 구분한다. 짐작뿐인 이미지는
  미확정으로 두고 등록하지 않는다.
- 성별은 지금 여성(`f`)만 만들지만 파일명에 반드시 적는다. 나중에 `m` 을 더해도 옛
  파일의 뜻이 안 바뀌게 하려는 것이다.
- 이미 등록된 이미지, 다른 작업자의 변경을 보존한다. 파일을 더하려고 기존 초상을
  덮어쓰거나 검사를 느슨하게 하지 않는다.
- 파일명·화풍 키·카드 이름은 기억으로 만들지 말고 저장소와 대조한다. 이 문서와
  코드가 다르면 먼저 차이를 확인한다.

## 2. 작업 전에 읽을 파일

| 파일 | 확인할 내용 |
|---|---|
| `data/card.json` | 카드의 정확한 이름 (도감 1,025장) |
| `data/style.json` | 허용된 화풍 키. `lib/prompt-spec.js` 의 `ART_STYLES` 에서 생성한다 |
| `data/img.json` | 이미 등록된 파일, 화풍별·폼별 자리, 컷 번호 |
| `tools/register-images.py` | 파일명 해석과 등록·검사 동작 |
| `tools/make-thumbs.py` | 썸네일 생성 |
| `docs/PROMPT_REWRITE.md` | 생성기의 세 출력과 기준 시트 규칙 |
| `pkm-atelier-img/README.md` | 그림 저장소의 자리와 올린 뒤 할 일 |

같은 세션에서 읽은 것은 바뀌지 않았다면 다시 읽지 않는다.

## 3. 한 캐릭터의 기본 구성 — 기준 시트 1 + 일곱 장

```
기준 시트 1장 → 일반 폼 3장 → 폼별 개방(폭주) 3장 → 일상컷 1장
```

| 순서 | 출력 | 인물 기준 | 장갑 폼 | 폭주 기준 장갑 | 첨부 | 결과 |
|---|---|---|---|---|---|---|
| 0 | 폼 초상 | 새 인물 만들기 | 경장 | — | 없음 | **기준 시트** (3:4, 정면·후면·확대) |
| ① | 폼 초상 | 확정한 이미지 이어가기 | 경장 | — | 기준 시트 | 경장 초상 |
| ② | 폼 초상 | 확정한 이미지 이어가기 | 중장 | — | 기준 시트 | 중장 초상 |
| ③ | 폼 초상 | 확정한 이미지 이어가기 | 고기동 | — | 기준 시트 | 고기동 초상 |
| ④ | 폼 초상 | 확정한 이미지 이어가기 | 폭주 | 경장 | 기준 시트 + ① | 경장 개방 초상 |
| ⑤ | 폼 초상 | 확정한 이미지 이어가기 | 폭주 | 중장 | 기준 시트 + ② | 중장 개방 초상 |
| ⑥ | 폼 초상 | 확정한 이미지 이어가기 | 폭주 | 고기동 | 기준 시트 + ③ | 고기동 개방 초상 |
| ⑦ | 일상컷 | 확정한 이미지 이어가기 | (안 탄다) | — | 기준 시트 | 일상컷 |

### 0. 기준 시트

- 생성기: 출력 **폼 초상**, 인물 기준 **새 인물 만들기**, 장갑 폼 **경장**, 화풍은
  앞으로 쓸 것 하나로 고정, 이미지 첨부 없음.
- 결과는 3:4 한 장에 정면·후면 전신과 얼굴·특징 장갑·등 장비·발의 확대컷이 같이 든다.
- 마음에 안 들면 새로 만든다. 마음에 드는 **하나만** 기준 시트로 확정한다. 첫 결과가
  자동으로 기준이 되지 않는다.
- 이 시트는 게임용 경장 초상이 아니다. ①을 따로 만든다.

### 1. 일반 폼 초상 셋 (①②③)

- 매번 기준 시트를 첨부하고 인물 기준을 **확정한 이미지 이어가기**로 둔다.
- 장갑 폼만 경장 → 중장 → 고기동으로 바꾼다.
- 셋은 도감에서 나란히 견주므로 **단일 인물, 전신 2:3, 비슷한 카메라와 피사체 크기**여야
  한다. 시트의 배치(정면·후면·확대)를 복제하지 않는다.

### 2. 폼별 개방 초상 셋 (④⑤⑥)

- 출력은 계속 **폼 초상**이다. 액션이 아니다.
- 장갑 폼 **폭주**, 폭주 기준 장갑을 경장 → 중장 → 고기동으로.
- 첨부는 둘: **첫째 기준 시트**(얼굴·머리·체형), **둘째 그 폼의 일반 초상**(장갑 구조).
  둘째를 붙일 수 없는 서비스면 기준 시트만으로 만들되 그 사실을 기록에 남긴다.
- 개방은 "기준 장갑의 틈·패널을 연다" 는 뜻이다. 모든 관절을 열거나 새 장갑을 덧붙인
  결과는 다시 만든다.

### 3. 일상컷 (⑦)

- 출력 **일상컷**, 기준 시트 첨부, 원하는 카테고리·의상·장면 선택. 장갑 폼은 적용되지
  않는다.
- 일반 폼이나 개방 이미지를 기준으로 삼지 않는다. 늘 기준 시트다.
- 일상 의상은 자동으로 코스프레가 되지 않는다. 원본 색이나 작은 장신구만 남겨도 된다.

### 액션은 나중, 선택

액션 모드는 필수 일곱 장 뒤에 만드는 선택적 연출컷이다. 전투용 기본 여섯 장(①~⑥)을
액션으로 만들지 않는다. 만든다면 폼마다 `actionN` 으로 등록한다(5절).

### 그 밖의 원칙

- 얼굴·머리·체형은 첨부한 시트가 정한다. 저장된 외형 값을 후속 프롬프트에 다시 실어
  덮어쓰지 않는다. 표정과 머리의 움직임은 바뀌어도 되지만 얼굴 구조와 머리 길이는
  안 바뀐다.
- 생성기가 주는 항목(원본 포켓몬·화풍·성인 여성 외형·장갑 폼·폭주 기준)은 실제 선택
  UI 에서 먼저 정한다. 선택지에 없는 것만 추가 지시로 보완하고, **실제 제출한 원문을
  이미지마다 1회 확인**한다. 기억으로 비슷한 프롬프트를 새로 쓰고 생성기를 썼다고
  보고하지 않는다.
- 사용자가 요청한 의상·분위기·구도를 임의로 완화하거나 다른 방향으로 바꾸지 않는다.

## 4. 저장 자리와 파일명

그림은 이 저장소에 두지 않는다. **`pkm-atelier-img`** 저장소의 `img/` 바로 아래에
올리고, 이 저장소의 `data/img.json` 에는 폴더 없이 파일 이름만 적는다.
`lib/img.js` 의 `BASE` 한 줄이 그 주소를 가리킨다.

| 용도 | 파일명 | 예 |
|---|---|---|
| 폼 초상 (①②③) | `<카드>_<폼>_<화풍>_f.webp` | `피카츄_light_cinematic_semi_real_f.webp` |
| 폼별 개방 초상 (④⑤⑥) | `<카드>_<폼>_overdrive_<화풍>_f.webp` | `피카츄_heavy_overdrive_cinematic_semi_real_f.webp` |
| 일상컷 (⑦) | `<카드>_<화풍>_f_casualN.webp` | `피카츄_cinematic_semi_real_f_casual1.webp` |
| 액션 (선택) | `<카드>_<폼>_<화풍>_f_actionN.webp` | `피카츄_mobility_cinematic_semi_real_f_action1.webp` |
| 기준 시트 (등록 안 함) | `sheets/<카드>_<화풍>_f_sheet.webp` | 그림 저장소의 `sheets/` — `img/` 밖 |

- 폼 키는 `light` · `heavy` · `mobility`. 개방은 `<폼>_overdrive` 로 폼을 앞에 둔다.
  폼 없는 `<카드>_overdrive_<화풍>_f.webp` 는 옛 자료 호환용이며 신규에는 쓰지 않는다
  (화면은 폼별 개방이 없을 때만 그것을 대신 쓴다).
- 카드 이름은 `data/card.json` 의 한글 이름 그대로. 공백은 밑줄로 써도 된다.
- `N` 은 1 이상. 같은 카드·화풍·성별·용도 안에서 기존 파일명을 보고 정한다.
- 폼 초상은 1024×1536 (2:3). 크기 조건이 따로 없으면 원본 크기를 유지하고, 비율을
  보존하며 자르거나 늘리지 않는다. 이미 WebP 면 다시 인코딩하지 않는다. 확장자만 바꾸지
  말고 실제 WebP 로 열리는지 확인한다.
- `style-` 로 시작하는 파일은 화풍 견본이라 등록에서 제외된다. 카드 그림에 이 접두사를
  쓰지 않는다.
- 초상 자리는 카드·화풍·폼·성별마다 하나다. 이미 있으면 덮어쓰거나 일상컷으로 위장하지
  않는다. 교체 의도인지 확인한다.

## 5. 분류표

원본마다 등록 전에 적어 둔다.

| 항목 | 내용 |
|---|---|
| 원본 | 받은 파일명 또는 접근 가능한 경로 |
| 카드 | `data/card.json` 의 정확한 이름 |
| 자리 | 폼 초상(light/heavy/mobility) · 개방(<폼>_overdrive) · casual N · action N |
| 화풍 | 확정 키 또는 미확정 |
| 근거 | 생성기 선택값 · 사용자 확정 · 외형 짐작 중 무엇인지 |
| 기준 | 어느 기준 시트로 만들었나, 개방이면 둘째 첨부가 어느 일반 초상인가 |
| 출력 | 최종 파일명, 픽셀 크기, 형식, 해시 |

같은 인물인지는 얼굴·머리·체형·장갑으로 대조하되 애매하면 확정하지 않는다.

## 6. 등록 순서 — 올리면 자동이다

썸네일과 등록은 워크플로가 한다. 사람이 하는 것은 **이름을 맞춰 그림 저장소에 올리는 것**
까지다.

| 어디 | 워크플로 | 언제 | 하는 일 |
|---|---|---|---|
| `pkm-atelier-img` | `썸네일` | `img/*.webp` 가 푸시되면 | `img/thumb/` 를 만들어 `[skip actions]` 로 되커밋 |
| `pkm-atelier` | `그림 등록` | 썸네일이 깨울 때 · 매시 17분 · 수동 실행 | 그림 저장소를 읽어 `data/img.json` 에 적고 되커밋 |

그림 저장소의 푸시가 코드 저장소를 직접 깨우지는 못한다(권한이 없다). 그래서 썸네일
워크플로가 끝나면서 토큰(`CODE_REPO_TOKEN`, 그림 저장소 README 에 만드는 법)으로 `그림
등록` 을 두드린다. 토큰이 없으면 시간표에 기대는데 GitHub 시간표는 자주 빠진다(8시간에
한 번 돈 적이 있다) — 그럴 땐 `pkm-atelier` 의 Actions 에서 `그림 등록` 을 **Run workflow**
로 누른다.

1. `pkm-atelier` 와 `pkm-atelier-img` 의 `main` 최신 커밋을 확인한다. 진행 중인 다른
   변경이 있으면 보존한다.
2. 분류표와 원본 수를 대조하고, 확정된 이미지에만 최종 파일명을 준다.
3. 그림 저장소의 `img/` 와 `data/img.json` 의 같은 자리를 모두 확인한다. 같은 이름의
   다른 내용은 자동으로 덮어쓰지 않는다.
4. 올리기 전에 이 저장소에서 이름을 검사한다 — 워크플로는 이름이 틀린 파일이 하나라도
   있으면 **전부** 등록하지 않는다(오타를 잡으려고 일부러 통과시키지 않는다).

```bash
mkdir -p img && cp <원본들> img/            # img/ 는 .gitignore 라 올라가지 않는다
python3 tools/register-images.py --check   # 쓰지 않고 무엇이 달라지는지만
node tests/forms.cjs
```

5. 그림 저장소에 원본을 올린다 — `img/` 아래에 최종 WebP, 기준 시트는 `sheets/` 에.
   한 캐릭터 분량을 **커밋 하나**로 `main` 에 직접 올린다. 작업 브랜치와 PR 은 만들지 않는다.
   `img/thumb/` 와 `data/img.json` 은 워크플로가 만드니 손으로 올리지 않는다.
6. 그림 저장소의 `썸네일` 실행이 초록인지, 코드 저장소의 `그림 등록` 이 돌아 `data/img.json`
   커밋이 생겼는지 본다. 실패했으면 로그를 읽고 원인(대개 파일 이름)을 고친 뒤 다시 올린다.
7. 실패·경고가 없으면 화면을 열어 보는 검수는 기본 절차가 아니다. 구체적인 문제가 있거나
   사용자가 요청할 때만 아래로 본다.

```bash
ESM_DIR=<node_modules> CHROMIUM_PATH=<chrome> node tests/dex-forms.cjs
ESM_DIR=<node_modules> CHROMIUM_PATH=<chrome> node tests/battle-screen.cjs
```

워크플로를 못 쓰는 자리(막힌 네트워크 등)에서만 손으로 한다 — 같은 도구를 같은 순서로:
`register-images.py` 로 `data/img.json` 을 적어 이 저장소에, `make-thumbs.py` 로 만든
`img/thumb/` 를 그림 저장소에 올린다.

- `--prune` 은 기본 명령이 아니다. 삭제·이동이 작업 범위에 있고 누락 원인을 확인했을
  때만 쓴다. 체크아웃이 불완전하다고 기존 항목을 지우지 않는다.
- 등록기는 파일 이름에서 화풍·폼·성별을 벗겨 내고 남은 것을 카드 이름으로 읽는다.
  오타는 "그런 카드가 없다" 로 걸리며 일부러 통과시키지 않는다.
- 얼굴 좌표(face)는 파일 이름으로는 알 수 없다. 등록기는 없는 것만 더하고 있는 것은
  건드리지 않는다.

## 7. 기존 이미지 이동·재분류

- 옛 자리(`semi_real`, 폼 없는 `overdrive`)에서 옮기려면 그 이미지의 화풍·폼 근거가
  있어야 한다. 카드 단위로 일괄 부여하지 않는다.
- 이름만 바꾸는 것이면 바이트를 그대로 쓴다(해시가 같아야 한다). 해시가 같다는 것은
  내용 보존의 증명이지 분류가 맞다는 증명은 아니다.
- 옛 이름 제거와 새 이름 등록이 그림 저장소·`img.json`·썸네일 세 곳에서 같이
  반영됐는지 확인한다.

## 8. 중단 대비와 재개

- 일곱 장을 한 번에 기다리지 말고 **묶음마다**(시트 확정 → ①②③ → ④⑤⑥ → ⑦) 분류표와
  체크포인트를 남긴다.
- 체크포인트에는 기준 시트의 실제 위치와 해시, 각 이미지의 원본/변환본 위치, 최종
  파일명, 해시, 어느 저장소의 어느 커밋까지 반영됐는지를 적는다.
- 원본과 변환본이 다른 세션에서 실제로 접근 가능한 곳에 있는지 확인한다. 임시 경로만
  적어 두고 복구 가능하다고 보고하지 않는다.
- 재개할 때는 체크포인트와 두 저장소의 원격 상태를 대조하고, 이미 있는 파일을 다시
  만들지 않는다. **기준 시트가 없으면 그 캐릭터의 후속 생성은 못 잇는다** — 시트를 못
  찾으면 ①부터 다시 만드는 것이 아니라 시트부터 확정한다.
- 이전 세션의 기록이 없으면 당시 프롬프트·화풍·업로드 성공 여부를 짐작해 채우지 않는다.

## 9. 완료 보고 기준

- 그림 저장소에 파일이 있다는 것만으로 완료가 아니다. 워크플로가 `data/img.json` 등록과
  썸네일을 두 저장소의 `main` 에 반영해야 한다. 그 되커밋(`[skip actions]`)은 정상이며
  "커밋 하나" 원칙의 예외다.
- 생성 수/요청 수(일곱 장 가운데 몇 장), 두 저장소의 커밋, 문제·대기 항목만 짧게
  보고한다. 정상 완료 시 검사 명령이나 단계별 상태를 나열하지 않는다.
- 그림 저장소에만 올렸으면 "올림·등록 대기", 등록까지 했으면 "등록 완료" 로 구분한다.
  직접 열어 보지 않은 배포 화면까지 완료로 넓히지 않는다.
- 로컬에서 검사를 못 돌렸으면 돌렸다고 보고하지 않는다. 원인을 적고 멈춘다.
